# Sample of AWS IoT Device SDK for JavaScript

Before running the script you need to solve the dependency:

```
$ wget https://raw.githubusercontent.com/aws/aws-iot-device-sdk-js/master/package.json package.json

$ npm install

$ node main.js
```
